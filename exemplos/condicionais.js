//alert("ok");

var hora = 13;

/*

var hora = 13;

if(hora < 12) {
 alert("Bom dia");
} else if (hora < 17) {
    alert('Boa Tarde');
} else if (hora < 23) {
    alert('boa noite');
}
*/
  //Duas condções dentro de um if
  //&& = Te possibilita colocar duas condiçoes dentro do mesmo if

  /*
   var hora = 13;

  if ( hora >= 12 && hora < 18 ) {
      alert('Boa Tarde');
  }
  */

  //Condicional if com dus condiçoes, neste caso se executará um ou outro, se uma das duas condicionais atender o requisito pedido pela condicional
  //o código será executado
  // este barra barra -> || representa a palavra ou ... o código será executado com alguma das duas condiçoes sendo true

  var hora = 8;

  if (hora == 7 || hora == 9 ) {
    alert('Vc tá no café da manhã');
  }

